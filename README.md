# LLM_RAG_SQL_Applicatoins
A demonstration application for Query RAG that showcases an end-to-end natural-language-to-SQL workflow over user-uploaded CSV data. The system ingests tabular files, profiles schema and column semantics, and builds a retrieval layer that grounds LLM reasoning in dataset-aware metadata rather than relying solely on parametric memory.
